.DS_Store
*.swp

# spec-run's own state, if this repo ever runs the pipeline on itself
specs/*/.pipeline/

# run logs, for tailing while a pipeline works
.runs/

# local worktrees, for running phases side by side
.worktrees/
